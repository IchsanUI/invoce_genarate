# /includes contains PHP libraries that are only meant to be
# included via require/include — never accessed directly.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
